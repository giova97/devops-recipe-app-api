---
name: Checks

on:
  pull_request:
    branches: [main]
      

jobs:
  test-lint:
    name: Test and Lint
    uses: ./.github/workflows/test-and-lint.yml
    secrets:
      DOCKERHUB_USER: ${{ vars.DOCKERHUB_USER }}  # Correcto si es una variable
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}  # Asegúrate de que esto sea un secreto

      
